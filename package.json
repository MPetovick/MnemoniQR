{
  "name": "radialstitch-pro",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "parcel public/index.html --port 3000",
    "build": "parcel build public/index.html --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "localforage": "^1.10.0"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}
